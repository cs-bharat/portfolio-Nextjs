/** @type {import('next').NextConfig} */
const nextConfig = {
    // next config systems //
    images:{
        domains:['images.unsplash.com' , 'res.cloudinary.com']
    }
};

export default nextConfig;
